All files in the lib/build directory are required ONLY for building of the deployment resources (not at runtime).

Library                             Required By
----------------------------------  ------------------------
